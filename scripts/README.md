# Scripts

- **`cli/`** — 与数据集无关的通用命令行工具（H5 转换、划分列表）。
- **`datasets/<name>/`** — 各竞赛/数据集的预处理与划分脚本；修改路径后可直接运行。
- **`tools/`** — 诊断、重命名、按列表移动文件等辅助脚本。
- **`legacy/`** — 历史脚本，行为未改，但依赖 `med_preprocess` 中的共享实现。

运行前请在仓库根目录执行 `pip install -e .`，或将仓库根目录加入 `PYTHONPATH`。
